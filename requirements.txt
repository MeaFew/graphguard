# PyTorch wheels with CUDA 12.1 (RTX 4060 Laptop)
--extra-index-url https://download.pytorch.org/whl/cu121

# Use the local '+cu121' version tag to request the CUDA wheels
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# Graph Neural Networks
torch-geometric>=2.4.0
-f https://data.pyg.org/whl/torch-2.5.0+cu121.html
torch-scatter
torch-sparse

# Traditional ML
xgboost>=2.0.0
scikit-learn>=1.3.0

# Data and visualization
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Dashboard
# Lower bound 1.43: dashboard/app.py uses width="stretch" (the replacement for
# use_container_width), which older releases don't support.
streamlit>=1.43.0
plotly>=5.18.0

# Data download (download_data.py imports kagglehub; without it the script
# silently falls back to the synthetic graph, which is NOT comparable to the
# real Elliptic benchmark — so this dep must be present for the real-data path).
kagglehub>=0.3.0

# Utilities
tqdm>=4.65.0
joblib>=1.3.0
networkx>=3.0

# Development tools (pytest/ruff/mypy/pre-commit) are declared in
# pyproject.toml's "dev" extra — install with: pip install -e ".[dev]"
