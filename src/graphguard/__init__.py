"""graphguard — GNN-based bitcoin fraud detection on the Elliptic dataset."""

from __future__ import annotations

import logging

__version__ = "1.0.0"

logging.getLogger(__name__).addHandler(logging.NullHandler())
